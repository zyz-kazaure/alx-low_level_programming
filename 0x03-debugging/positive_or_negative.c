#include "main.h"
#include <stdio.h>
/**
 * positive_or_negative - ndjjd
 *
 * @i: djdjdjd
 *
 * Return: djdjsdk
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}
